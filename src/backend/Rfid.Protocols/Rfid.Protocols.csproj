<Project Sdk="Microsoft.NET.Sdk">

  <!-- Reader wire formats and the ingest contract, shared by the server and the edge agent. No EF, no ASP.NET. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
